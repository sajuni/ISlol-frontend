export default class User {
    constructor(email, name, roles, pwd, addr){
        this.email = email;
        this.name = name;
        this.roles = roles;
        this.pwd = pwd;
        this.addr = addr;
    }
}